export type FormikErrorType = {
    email?: string
    password?: string
    confirmPassword?: string
    rememberMe?: boolean
}

export type dataType = {
    email: string
    password: string
    confirmPassword: string
}
